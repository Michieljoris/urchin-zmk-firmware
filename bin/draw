set -e

#https://github.com/caksoylar/keymap-drawer
#pipx install keymap-drawer

rm -f urchin_keymap.ortho.svg
uvx --from keymap-drawer keymap -c draw2.config draw keymap.yaml >urchin_keymap.ortho.svg
